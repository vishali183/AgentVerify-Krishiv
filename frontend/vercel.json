{
  "framework": "nextjs",
  "installCommand": "npm install && npm --prefix ../backend install && unset PRISMA_QUERY_ENGINE_LIBRARY && npm --prefix ../backend run db:generate",
  "buildCommand": "npm run build"
}